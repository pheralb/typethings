"use client";

import React from "react";
import Snowfall from "react-snowfall";
import type { SnowfallProps } from "react-snowfall";

const SnowfallComponent = (props: SnowfallProps) => {
  return <Snowfall {...props} />;
};

export default SnowfallComponent;
